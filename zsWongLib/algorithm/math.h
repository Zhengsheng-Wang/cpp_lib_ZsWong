#pragma once
#ifndef ZSWONG_MATH
#define ZSWONG_MATH

#include <cmath>

template <typename T> bool is_prime_number(const  T &_T_ori){
    //Judge whether a integer _T_ori is prime. 
    //User should make sure that _T_ori is integer rather than any other kind of type
    T _T_up_bound = std::sqrt(_T_ori);
    return false;
}

#endif